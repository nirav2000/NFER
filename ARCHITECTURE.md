# ARCHITECTURE

Static architecture:
- HTML pages at repository root
- shared CSS in `css/styles.css`
- modular browser logic in `js/*.js`
- content in `data/passages/*.json` and `data/questions/*.json`
- LocalStorage for persistence

No backend or build step is required.
